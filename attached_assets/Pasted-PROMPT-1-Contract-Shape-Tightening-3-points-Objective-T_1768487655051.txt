PROMPT 1 — Contract/Shape Tightening (3 points)

Objective: Tighten RCL v0.1 contract conformance for instruments + screen payload + snapshot/export wiring. Do not add any new features beyond contract alignment. Keep everything read-only.

Scope (do all 3):

1) /instruments response + provider alignment
- Ensure GET /api/rcl/v0.1/instruments returns:
  {
    "items": [...],
    "pagination": { "limit": <number>, "next_cursor": <string|null> }
  }
- Confirm the mock provider function getInstruments(...) returns { items, next_cursor } (NOT { instruments, next_cursor }).
- If the provider currently returns { instruments, next_cursor }, either:
  (A) rename the provider return to { items, next_cursor } OR
  (B) map instruments->items in the route.
- Update any client code that reads instruments accordingly.
- Do not break pagination.

2) /screen/adgm time_mode enum strictness
- Enforce RclTimeMode enum strictly as: "latest_snapshot" | "at_time"
- Never emit "realtime" anywhere (including in JSON exports).
- In GET /api/rcl/v0.1/screen/adgm:
  - Accept query param time_mode; if time_mode === "at_time" then require `at` ISO timestamp, else default to "latest_snapshot".
  - Pass the enum through to the payload generator so meta.time_mode always matches exactly.

3) Snapshot immutability & export wiring check
- Confirm the snapshot_ref generated by /screen/adgm is cached immediately via cacheSnapshot(payload).
- Confirm /exports/:snapshot_ref/json and /exports/:snapshot_ref/pdf read the cached snapshot by snapshot_ref.
- Exports must return the exact frozen payload used for rendering (do not regenerate).
- If snapshot_ref is missing from cache, exports must return a 404 error with a consistent error object.

Deliverable:
- Make the smallest possible code changes to achieve strict contract alignment.
- Ensure TypeScript types reflect the contract (no loose string time_mode).
- Run/build should still pass.

Non-negotiables:
- No POST/PUT/PATCH/DELETE.
- No trading UI features.
- Do not change the top-level payload blocks: meta, access_context, header, coverage, truth, provenance, export.